[project]
name = "basic-example"
version = "0.0.1"
requires-python = ">=3.10"
dependencies = [
    "judgeval",
    "openai>=2.29.0",
]

[tool.uv.sources]
judgeval = { path = "../../", editable = true }
