[project]
name = "claude-agent-sdk-example"
version = "0.0.1"
requires-python = ">=3.10"
dependencies = [
    "judgeval",
    "claude-agent-sdk",
]

[tool.uv.sources]
judgeval = { path = "../../", editable = true }
